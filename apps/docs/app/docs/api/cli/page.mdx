---
title: "CLI"
description: "Commands and flags for running repository workflows from the terminal."
order: 60
---

# CLI

Use this section to find the CLI pages for Supabase Auth Seeder.

## When to Use This Section
Use this section when you need to run Supabase Auth Seeder from the terminal. The pages here explain command purpose, required configuration, supported flags, safe defaults, and what to verify after the command finishes.

## Available Pages
- [Seed Command](/docs/api/cli/seed) - The CLI exposes a seeding workflow and runtime flags.

## Reading Order
- Start with the command purpose before copying flags.
- Check required environment variables before running commands that create, mutate, or seed data.
- Prefer the smallest safe script first, then increase scale or concurrency after verification.
